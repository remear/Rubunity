!!!
%html{:xmlns => "http://www.w3.org/1999/xhtml"}
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}
    %title Rubunity
    - if params[:controller] == 'bookmarks'
      = stylesheet_link_tag 'bookmarks.css'
    = stylesheet_link_tag 'redesign.css'
    #header
      .header_holder
        .logo
        #nav.bookmarks
          = render :partial => 'layouts/gnav'
        .divider
    #toolbar
      %ul#breadcrumbs
        %li#home= link_to '&nbsp;'.html_safe, '/'
        = yield :breadcrumbs
      %ul#actions
        = yield :actions
      .clearer
    #content
      .content_holder
        = yield
    #footer_bottom
      .holder
        .copy
          Copyright ©
          = succeed "." do
            %a{:href => "http://test.nattywp.com/business"} business Demo
          All Rights Reserved.
        .links
          = render :partial => 'layouts/gnav'
        %div{:style => "clear:both;"}